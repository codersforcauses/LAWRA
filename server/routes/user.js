import express from 'express'

const router = express.Router()

//Prints Hello with path
// router.get("/:id", (req, res) => {
//     res.send("Hello " + req.params.id + "!")
// })

// module.exports = router()

export default router
